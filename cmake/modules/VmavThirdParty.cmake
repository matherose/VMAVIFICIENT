# VmavThirdParty.cmake — vendored static deps orchestrator.
#
# Phase 0 stub. Real implementation arrives in Phase 1/3 once each
# submodule under third_party/ has been added and a per-dep
# ExternalProject_Add or add_subdirectory wrapper is plumbed in.
#
# Expected layout (filled incrementally):
#   third_party/ffmpeg/, SVT-AV1-HDR/, libvmaf/, libdovi/, libhdr10plus/,
#   libopus/, libcurl/, openssl/, cjson/, tesseract/, leptonica/,
#   libpng/, libjpeg-turbo/, libtiff/, zlib/, unity/, cmock/

include_guard(GLOBAL)

message(STATUS "VmavThirdParty: stub (Phase 0). No deps built yet.")
